{
  "name": "tideways",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "PNW ferry management sim — design prototype",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "esbuild test/smoke.ts --bundle --platform=node --format=cjs --outfile=test/.smoke.cjs && node test/.smoke.cjs"
  },
  "devDependencies": {
    "esbuild": "^0.25.12",
    "typescript": "^5.6.3",
    "vite": "^6.0.7"
  }
}
